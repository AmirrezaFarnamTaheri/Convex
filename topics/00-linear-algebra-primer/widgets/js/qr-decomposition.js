// Placeholder for qr-decomposition widget
export function initQrDecomposition(containerId) {
    const container = document.getElementById(containerId);
    container.innerHTML = "<p><i>QR decomposition widget will be implemented here.</i></p>";
}
