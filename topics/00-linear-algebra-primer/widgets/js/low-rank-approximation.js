// Placeholder for low-rank-approximation widget
export function initLowRankApproximation(containerId) {
    const container = document.getElementById(containerId);
    container.innerHTML = "<p><i>Low-rank approximation widget will be implemented here.</i></p>";
}
